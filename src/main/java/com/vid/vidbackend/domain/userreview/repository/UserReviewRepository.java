package com.vid.vidbackend.domain.userreview.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserReviewRepository extends JpaRepository<Long, UserReviewRepository> {
}
